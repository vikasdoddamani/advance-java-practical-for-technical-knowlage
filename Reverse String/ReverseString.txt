



STEP 1. Create a temporary byte[]  of length equal 
        to the length of the input string.
STEP 2. Store the bytes (which we get by using 
        getBytes() method) in reverse order into 
        the temporary byte[] .
STEP 3. Create a new String abject using byte[] to
        store result.